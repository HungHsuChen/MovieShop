@model IEnumerable<ApplicationCore.Models.MovieCardResponseModel>

@{
    ViewData["Title"] = "Home Page";
}

<div class="container-fluid">
    <div class="row">
        @foreach (var movie in Model)
        {
            @*<div>
                @movie.Id
            </div>

            <div>
                @movie.Title
            </div>

            <div>
                <img src = "@movie.PosterUrl"/>
            </div>*@

            <div class="col-xl-2 col-sm-4 col-lg-3 col-6 mb-4">
                @*call partial view and pass the model to the partial view*@
                <partial model ="movie" name="_MovieCard"/> 
            </div>

            
        }
    </div>
</div>


